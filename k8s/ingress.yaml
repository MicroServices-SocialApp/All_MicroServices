apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      
      # --- USER API ROUTE ---
      - path: /user(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: user-api
            port:
              number: 80
      
      # --- POST API ROUTE ---
      - path: /post(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: post-api
            port:
              number: 80
      
      # --- COMMENT API ROUTE ---
      - path: /comment(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: comment-api
            port:
              number: 80
      
      # --- AUTH API ROUTE ---
      - path: /auth(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: auth-api
            port:
              number: 80