apiVersion: apps/v1
kind: Deployment
metadata:
  name: comment-api
spec:
  selector:
    matchLabels:
      app: comment-api
  template:
    metadata:
      labels:
        app: comment-api
    spec:
      initContainers:
      - name: db-migration
        image: comment-api:local
        imagePullPolicy: Never
        command: ["alembic", "upgrade", "head"]
        env:

        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_USERNAME

        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_PASSWORD

        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_DB_NAME
        - name: COMMENT_DATABASE_URL
          value: "postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@comment-db:5432/$(POSTGRES_DB)"
      # --- End of InitContainer ---
      
      containers:
      - name: comment-api
        image: comment-api:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"   # 10% of a CPU core
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:

        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: SECRET_KEY
        
        - name: ALGORITHM
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: ALGORITHM

        - name: POSTGRES_USERNAME
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_USERNAME

        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_PASSWORD

        - name: POSTGRES_DB_NAME
          valueFrom:
            secretKeyRef:
              name: comment-api-secrets
              key: POSTGRES_DB_NAME
        # Constructing the URL: comment-db is the service name from comment-db.yaml
        - name: COMMENT_DATABASE_URL
          value: "postgresql+asyncpg://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@comment-db:5432/$(POSTGRES_DB_NAME)"
---
apiVersion: v1
kind: Service
metadata:
  name: comment-api
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: comment-api