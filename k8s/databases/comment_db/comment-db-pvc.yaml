apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comment-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: comment-db-pv
  resources:
    requests:
      storage: 5Gi # Change this based on your data needs